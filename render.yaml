services:
  - type: web
    name: lofselect-gescon-action
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ACTION_API_KEY
        sync: false
